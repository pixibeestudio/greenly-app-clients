<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F5F5F5"
    android:fitsSystemWindows="true"
    tools:context=".ui.fragment.FilteredProductsFragment">

    <!-- ==================== HEADER ==================== -->
    <LinearLayout
        android:id="@+id/layoutHeader"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="@color/white"
        android:elevation="4dp"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingHorizontal="12dp"
        android:paddingVertical="8dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <!-- Nút quay về Home -->
        <ImageButton
            android:id="@+id/btnBackHome"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="Quay lại"
            android:scaleType="centerInside"
            android:src="@drawable/ic_arrow_back"
            app:tint="@color/black" />

        <!-- Ô tìm kiếm giả -->
        <TextView
            android:id="@+id/tvSearchBox"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_marginHorizontal="10dp"
            android:layout_weight="1"
            android:background="@drawable/bg_search_input"
            android:drawableEnd="@drawable/ic_search"
            android:drawablePadding="8dp"
            android:gravity="center_vertical"
            android:hint="Tìm kiếm..."
            android:paddingHorizontal="16dp"
            android:textColor="@color/text_black"
            android:textColorHint="#9E9E9E"
            android:textSize="14sp" />

        <!-- Nút Giỏ hàng -->
        <FrameLayout
            android:layout_width="40dp"
            android:layout_height="40dp">

            <ImageButton
                android:id="@+id/btnCart"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:contentDescription="Giỏ hàng"
                android:scaleType="centerInside"
                android:src="@drawable/ic_cart"
                app:tint="@color/black" />

            <!-- Badge số lượng giỏ hàng (ẩn mặc định) -->
            <TextView
                android:id="@+id/tvCartBadge"
                android:layout_width="18dp"
                android:layout_height="18dp"
                android:layout_gravity="top|end"
                android:layout_marginTop="2dp"
                android:layout_marginEnd="2dp"
                android:background="@drawable/bg_badge_red"
                android:gravity="center"
                android:textColor="@color/white"
                android:textSize="10sp"
                android:textStyle="bold"
                android:visibility="gone"
                tools:text="3"
                tools:visibility="visible" />
        </FrameLayout>
    </LinearLayout>

    <!-- Đường kẻ phân cách -->
    <View
        android:id="@+id/dividerHeader"
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:background="@color/shipper_divider"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/layoutHeader" />

    <!-- ==================== THANH FILTER "BIẾN HÌNH" ==================== -->
    <HorizontalScrollView
        android:id="@+id/svFilterBar"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:background="@color/white"
        android:elevation="2dp"
        android:paddingHorizontal="12dp"
        android:paddingVertical="8dp"
        android:scrollbars="none"
        android:clipChildren="false"
        android:clipToPadding="false"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/dividerHeader">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:clipChildren="false"
            android:clipToPadding="false">

            <!-- Nút Icon Filter (phễu lọc) -->
            <FrameLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginEnd="8dp"
                android:clipChildren="false"
                android:clipToPadding="false">

                <ImageButton
                    android:id="@+id/btnOpenFilter"
                    android:layout_width="38dp"
                    android:layout_height="36dp"
                    android:background="@drawable/bg_filter_icon_outline"
                    android:contentDescription="Mở bộ lọc"
                    android:scaleType="centerInside"
                    android:src="@drawable/ic_filter"
                    app:tint="#616161" />

                <!-- Badge đỏ hiển thị số bộ lọc đang áp dụng -->
                <TextView
                    android:id="@+id/tvFilterBadge"
                    android:layout_width="18dp"
                    android:layout_height="18dp"
                    android:layout_gravity="top|end"
                    android:translationX="6dp"
                    android:translationY="-6dp"
                    android:background="@drawable/bg_badge_red"
                    android:gravity="center"
                    android:textColor="@color/white"
                    android:textSize="10sp"
                    android:textStyle="bold"
                    android:visibility="gone"
                    tools:text="2"
                    tools:visibility="visible" />
            </FrameLayout>

            <!-- Nút Sort: "Lọc theo" -->
            <TextView
                android:id="@+id/btnCurrentSort"
                android:layout_width="wrap_content"
                android:layout_height="36dp"
                android:layout_marginEnd="8dp"
                android:background="@drawable/bg_filter_tag_outline"
                android:drawableEnd="@drawable/ic_arrow_drop_down"
                android:drawablePadding="4dp"
                android:gravity="center_vertical"
                android:paddingHorizontal="14dp"
                android:text="Lọc theo"
                android:textColor="#616161"
                android:textSize="13sp" />

            <!-- Nút Category: "Loại hàng" -->
            <TextView
                android:id="@+id/btnCurrentCategory"
                android:layout_width="wrap_content"
                android:layout_height="36dp"
                android:layout_marginEnd="8dp"
                android:background="@drawable/bg_filter_tag_outline"
                android:drawableEnd="@drawable/ic_arrow_drop_down"
                android:drawablePadding="4dp"
                android:gravity="center_vertical"
                android:paddingHorizontal="14dp"
                android:text="Loại hàng"
                android:textColor="#616161"
                android:textSize="13sp" />

            <!-- Nút Discount: "Ưu đãi" -->
            <TextView
                android:id="@+id/btnCurrentDiscount"
                android:layout_width="wrap_content"
                android:layout_height="36dp"
                android:background="@drawable/bg_filter_tag_outline"
                android:drawableEnd="@drawable/ic_arrow_drop_down"
                android:drawablePadding="4dp"
                android:gravity="center_vertical"
                android:paddingHorizontal="14dp"
                android:text="Ưu đãi"
                android:textColor="#616161"
                android:textSize="13sp" />
        </LinearLayout>
    </HorizontalScrollView>

    <!-- Đường kẻ phân cách -->
    <View
        android:id="@+id/dividerFilter"
        android:layout_width="0dp"
        android:layout_height="1dp"
        android:background="@color/shipper_divider"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/svFilterBar" />

    <!-- ==================== DANH SÁCH SẢN PHẨM ==================== -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvFilteredProducts"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:clipToPadding="false"
        android:padding="8dp"
        android:paddingBottom="20dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/dividerFilter"
        tools:listitem="@layout/item_product_grid" />

    <!-- Thông báo không tìm thấy sản phẩm (ẩn mặc định) -->
    <TextView
        android:id="@+id/tvEmptyFilter"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Không tìm thấy sản phẩm phù hợp"
        android:textColor="#9E9E9E"
        android:textSize="16sp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/dividerFilter" />

</androidx.constraintlayout.widget.ConstraintLayout>
